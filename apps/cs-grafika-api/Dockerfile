# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build cs-grafika-api`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t cs-grafika-api`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system cs-grafika-api && \
          adduser --system -G cs-grafika-api cs-grafika-api

COPY dist/apps/cs-grafika-api cs-grafika-api
RUN chown -R cs-grafika-api:cs-grafika-api .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix cs-grafika-api --omit=dev -f install

CMD [ "node", "cs-grafika-api" ]
